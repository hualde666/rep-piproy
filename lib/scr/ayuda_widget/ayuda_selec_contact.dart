import 'package:flutter/material.dart';

class AyudaSeleccionContactos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Container(
      child: Text(
        'Seleccion Contactos de Emergencia',
        style: TextStyle(fontSize: 30),
      ),
    ));
  }
}
