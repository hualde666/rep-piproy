import 'package:flutter/material.dart';

class EditarContacto extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Editar Contacto'),
        ),
        body: Container(
          child: Text('CONTACO xx'),
        ));
  }
}
